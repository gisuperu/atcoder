#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, m;
    cin >> n >> m;
    int A[n];
    // vector<int> A(n);
    for(int i = 0; i < n; i++){
        int a;
        cin >> a;
        A[i] = a; 
        
    }
    
}
