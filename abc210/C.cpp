#include <bits/stdc++.h>
using namespace std;

int main(){
    int n, k;
    cin >> n >> k;
    int c[n];
    map<int, bool> get;
    get.clear();
    for(int i = 0; i < n; i++){
        int t;
        cin >> t;
        if(i < k) get[t] = true;
        else if()
    }

}